# aws-docker-terraform-ansible
This is a devops project which will create an ec2 instance using terraform and will install docker In that ec2 instance which will be manage code for ansible.
I am adding this extra line of code because i want to trigger the on push event of branch main.
ß